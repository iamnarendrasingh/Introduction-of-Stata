*What is comment and how this works?

*All things which types in Green or after *(Star) symbol 
* is comment like this.

*sysuse auto 

*Above is a command which will import auto dataset. However , It is comment 
*So it will not load any dataset.

